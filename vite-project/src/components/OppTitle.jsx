// src/components/OpportunityTitle.jsx
import React from 'react';

const OpportunityTitle = ({ title }) => (
  <section className="opportunity-title">
    <div className="container">
      <h1>{title}</h1>
    </div>
  </section>
);

export default OpportunityTitle;
